name: sandbox-lua
root: ~/sandboxes/sandbox-lua

startup_pane: 2

windows:
  - editor:
      pre:
        - touch index.lua
      layout: even-horizontal
      panes:
        - make lua --makefile=../Makefile
        - sleep 0.5 && nvim index.lua
