name: sandbox-lua
root: ~/sandboxes/sandbox-lua

startup_pane: 2

windows:
  - editor:
      layout: even-horizontal
      panes:
        - make lua --makefile=../Makefile
        - sleep 0.5 && nvim main.lua
