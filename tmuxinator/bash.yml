name: sandbox-bash
root: ~/sandboxes/sandbox-bash

startup_pane: 2

windows:
  - editor:
      layout: even-horizontal
      panes:
        - make bash --makefile=../Makefile
        - sleep 0.5 && nvim main.bash
