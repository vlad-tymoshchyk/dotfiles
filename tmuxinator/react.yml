# /home/vlad/.config/tmuxinator/sandbox-react.yml

name: react
root: ~/sandboxes/sandbox-react

windows:
  - editor:
      layout: even-horizontal
      panes:
        - nvim src/index.js
  - npm:
      layout: even-vertical
      panes:
        - npm start
        - google-chrome --new-window http://localhost:3100
